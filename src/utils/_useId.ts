// "use client";

// import { useMemo } from "react";

// import { randomId } from "./_string";

// export const useId = () => {
//   const id = useMemo(() => "id-" + randomId(), []);
//   return id;
// };
